# 调和函数的均值性质逆命题证明

## 问题陈述

设 $\Omega$ 是一个有界区域，$u$ 是 $\Omega$ 上的连续函数，对任意 $x \in \Omega$，存在半径为 $r(x)$ 的球 $B_r$，使得 $B_r$ 紧包含于 $\Omega$，且 $u(x)$ 等于 $u$ 在 $B_r$ 上的积分平均值。求证 $u$ 是调和函数。

---

## 先把困难点说清楚

如果假设的是「对**每一个** $r<\operatorname{dist}(x,\partial\Omega)$ 都有 $u(x)=\frac{1}{|B_r(x)|}\int_{B_r(x)}u$」，那是均值定理的经典逆命题，用磨光/Poisson 积分几行就完。这里只给了**每点一个半径** $r(x)$，性质是"非局部"的：它**不能被子区域继承**（见文末注 2），所以证明必须在整个 $\Omega$ 上做比较。相应地，需要用到 $\Omega$ 有界这个条件以及 $u$ 在 $\overline\Omega$ 上连续（即 $u\in C(\overline\Omega)$，这是该定理即 Volterra–Kellogg 定理的标准假设，见注 3）。下面按此假设给出完整证明。

记积分平均值为 $\displaystyle\frac{1}{|B|}\int_B u\,dy$。已知：$\Omega\subset\mathbb R^n$ 为有界区域（开、连通），$u\in C(\overline\Omega)$，且

$$\forall x\in\Omega,\ \exists\, r(x)>0:\quad \overline{B_{r(x)}(x)}\subset\Omega,\qquad u(x)=\frac{1}{|B_{r(x)}(x)|}\int_{B_{r(x)}(x)}u .$$

---

## 第一步：单半径均值性质下的强最大值原理

**引理.** 设 $u$ 如上，$v\in C(\Omega)$ 在 $\Omega$ 内**次调和**（例如调和）。若 $w:=v-u$ 在某点 $x_0\in\Omega$ 达到 $m:=\sup_\Omega w<\infty$，则 $w\equiv m$ 于 $\Omega$。

**证明.** 取 $r=r(x_0)$，$B=B_r(x_0)\subset\subset\Omega$。次调和函数的体积均值不等式给出 $v(x_0)\le\frac{1}{|B|}\int_B v$，而 $u(x_0)=\frac{1}{|B|}\int_B u$，故

$$m=v(x_0)-u(x_0)\ \le\ \frac{1}{|B|}\int_B v-\frac{1}{|B|}\int_B u=\frac{1}{|B|}\int_B w\ \le\ m .$$

于是 $\frac{1}{|B|}\int_B (m-w)=0$，被积函数连续非负，故 $w\equiv m$ 于 $B$。这说明 $E=\{x\in\Omega: w(x)=m\}$ 是**开集**；由 $w$ 连续它又在 $\Omega$ 中闭；$\Omega$ 连通且 $E\neq\varnothing$，故 $E=\Omega$。$\square$

**关键之处：** $B_{r(x_0)}(x_0)\subset\subset\Omega$ 只保证 $u$ 的均值等式与 $v$ 的次均值不等式在**同一个球**上都可用，因此 $v$ 必须在**整个 $\Omega$** 上次调和。

---

## 第二步：比较原理

**命题.** 若 $v\in C(\overline\Omega)$ 在 $\Omega$ 内次调和且 $v\le u$ 于 $\partial\Omega$，则 $v\le u$ 于 $\Omega$。

**证明.** $w=v-u\in C(\overline\Omega)$，$\overline\Omega$ 紧，故 $\max_{\overline\Omega}w$ 在某 $x_*\in\overline\Omega$ 达到。若 $x_*\in\partial\Omega$，则 $\max w\le 0$。若 $x_*\in\Omega$，由引理 $w\equiv m$ 于 $\Omega$，再由 $\overline\Omega$ 上的连续性得 $w\equiv m$ 于 $\partial\Omega$，而那里 $w\le0$，故 $m\le0$。两种情形都有 $w\le0$。$\square$

把上述结论用于 $-u,-v$（注意 $-u$ 同样满足单半径均值性质）即得对称结论：若 $v\in C(\overline\Omega)$ 在 $\Omega$ 内超调和且 $v\ge u$ 于 $\partial\Omega$，则 $v\ge u$ 于 $\Omega$。

---

## 第三步：与 Dirichlet 问题的解比较

设 $h\in C(\overline\Omega)$ 满足 $\Delta h=0$ 于 $\Omega$，$h=u$ 于 $\partial\Omega$（边界数据 $u|_{\partial\Omega}$ 连续；当 $\partial\Omega$ 正则时——球、光滑域、Lipschitz 域等——这样的 $h$ 存在，一般有界域见注 1）。$h$ 既次调和又超调和，且在 $\partial\Omega$ 上与 $u$ 相等，由第二步的两个方向得

$$h\le u\quad\text{且}\quad h\ge u\ \Longrightarrow\ u\equiv h\ \text{于}\Omega .$$

故 $u\in C^\infty(\Omega)$ 且 $\Delta u=0$，即 $u$ 调和。$\blacksquare$

---

## 注释

### 注 1（任意有界域）

不假设 $\partial\Omega$ 正则时，取 $f=u|_{\partial\Omega}$ 的 Perron–Wiener–Brelot 上、下解 $\overline H_f,\underline H_f$。对下类中任一上半连续次调和 $v$（$\limsup_{x\to\xi}v\le f(\xi)$），$w=v-u$ 上半连续且 $\limsup_{x\to\partial\Omega}w\le0$；若 $\sup_\Omega w>0$，则该上确界必在 $\Omega$ 的紧子集上达到，引理（上半连续版本：由 $\frac{1}{|B|}\int_B(m-w)=0$ 得 $w=m$ a.e. 于 $B$，再用次调和函数 $v(x)=\limsup_{y\to x}v(y)$ 与 $u$ 连续把它升级为处处成立）给出 $w\equiv m>0$，与边界条件矛盾。故 $v\le u$，即 $\underline H_f\le u$；同理 $u\le\overline H_f$。Wiener 可解性定理说连续边值可解，$\underline H_f=\overline H_f=:h$ 调和，于是 $u=h$。

### 注 2（为什么不能局部化）

常见的错误证明是取 $\Omega'\subset\subset\Omega$、在 $\Omega'$ 上解 Dirichlet 问题得 $h$，再对 $u-h$ 用最大值原理。漏洞在于：$x\in\Omega'$ 的均值球只保证 $B_{r(x)}(x)\subset\subset\Omega$，可能伸出 $\Omega'$，此时 $\frac{1}{|B_{r(x)}(x)|}\int_{B_{r(x)}(x)}h=h(x)$ 无从谈起。单半径均值性质不被子区域继承，所以比较必须在整个 $\Omega$ 上进行。

### 注 3（假设的必要性）

上述结论即 Volterra（1909）、Kellogg（1934）的定理，$u\in C(\overline\Omega)$ 是其标准假设。若只假设 $u$ 在开集 $\Omega$ 上连续，问题要深刻得多，这就是 Littlewood 的"单圆问题"：对**有界**的 $u$ 有肯定结果（Hansen–Nadirashvili 等），而对无界的连续 $u$ 文献中已有反例。所以这里用到 $\overline\Omega$ 紧、$u$ 连续到边界，并非可有可无的技术细节。

---

## 总结

本证明通过三个步骤建立了单半径均值性质与调和性的等价关系：

1. **强最大值原理**：利用单半径均值性质与次调和函数的体积均值不等式
2. **比较原理**：建立满足单半径均值性质的函数与次调和/超调和函数的大小关系
3. **与调和函数比较**：通过 Dirichlet 问题的解夹逼出 $u$ 必为调和函数

证明的核心难点在于单半径均值性质的非局部性，这要求我们必须在整个有界区域上进行全局比较，而不能使用局部化技巧。
